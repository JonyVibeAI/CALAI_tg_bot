#!/bin/sh
set -e

# Схема БД уже применена, пропускаем db push при каждом старте
# Для применения изменений схемы запусти локально: npx prisma db push

echo "✅ Запуск бота..."

# Запускаем команду из CMD
exec "$@"
